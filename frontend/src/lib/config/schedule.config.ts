/** Длительность подынтервала съёмки по умолчанию (секунды) */
export const DEFAULT_SHOOTING_DURATION = 420;

/** Буфер безопасности рядом с засветкой (секунды) */
export const ZASVETKA_SAFETY_BUFFER = 60;

/** Минимальная визуальная длительность интервала на сетке (секунды) */
export const MIN_VISIBLE_DURATION = 300;

/** Номер КА по умолчанию */
export const DEFAULT_NUM_KA = 1525;

/** ID ограничений юстировки ОНА */
export const ONA_CONSTRAINT_IDS = [77, 78];

/** Шаг между подынтервалами для штатных съёмок (секунды) */
export const SHOOTING_STEP_STANDARD = 30 * 60;

/** Шаг между подынтервалами для учащённых съёмок (секунды) */
export const SHOOTING_STEP_ACCELERATED = 15 * 60;

/** Длительность подынтервала съёмки (минуты) */
export const SUB_INTERVAL_DURATION_MIN = 7;

/** Количество секунд в сутках */
export const SECONDS_IN_DAY = 86400;

/** Длительность режима по умолчанию (секунды) */
export const DEFAULT_MODE_DURATION = 300;